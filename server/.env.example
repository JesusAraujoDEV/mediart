# -----------------------------------------------------------------------------
# Configuración Principal de la Aplicación
# Estas configuraciones controlan el comportamiento fundamental de la aplicación.
# -----------------------------------------------------------------------------
NODE_ENV=development
# NODE_ENV=production # Descomentar para despliegue en producción y aplicar SSL/TLS
PORT=3000

# URL del lado del cliente (para aplicaciones frontend o whitelist de CORS)
CLIENT_URL=

# URL del lado del backend (para documentación de Swagger, callbacks externos, etc.)
# IMPORTANTE: Actualiza esto a la URL de tu backend en producción al desplegar.
BACKEND_URL=

# Whitelist de URLs para CORS, separadas por comas. Incluye todos los orígenes permitidos.
CORS_WHITELIST=

# -----------------------------------------------------------------------------
# Configuración de Seguridad y Autenticación
# Crítico para proteger tu aplicación y los datos de usuario.
# -----------------------------------------------------------------------------
API_KEY=
JWT_SECRET=

# -----------------------------------------------------------------------------
# Configuración de la Base de Datos (PostgreSQL)
# Detalles de conexión para tu base de datos PostgreSQL.
# Las URLs de desarrollo local y Docker están comentadas para ejemplos de despliegue en producción.
# -----------------------------------------------------------------------------
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB_URL=
# Ejemplo de URL de Producción (descomentar y reemplazar con tu URL de DB real de producción)
# POSTGRES_DB_URL=
DOCKER_POSTGRES_URL=

# Certificado CA de PostgreSQL para conexiones seguras (Aiven, Render, etc.)
# !!! PEGA EL CERTIFICADO COMPLETO AQUÍ Y DESCOMENTA LA LÍNEA !!!
# POSTGRES_CA_CERT=

# -----------------------------------------------------------------------------
# Claves y Credenciales de APIs Externas
# Secretos para servicios de terceros.
# -----------------------------------------------------------------------------
TMDB_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
GOOGLE_BOOKS_API_KEY=
GOOGLE_GEMINI_API_KEY=
DEEPSEEK_API_KEY=
IMGBB_API_KEY=

# -----------------------------------------------------------------------------
# Configuración de Email
# Configuraciones para el envío de correos (ej. recuperación de contraseña, notificaciones).
# -----------------------------------------------------------------------------
NO_REPLY_EMAIL=
EMAIL_APP_PASSWORD=

# -----------------------------------------------------------------------------
# API de Videojuegos (Twitch/IGDB)
# Credenciales específicas para APIs relacionadas con juegos.
# -----------------------------------------------------------------------------
VIDEOGAMES_CLIENT_ID=
VIDEOGAMES_CLIENT_SECRET=