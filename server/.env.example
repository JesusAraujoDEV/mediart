# -----------------------------------------------------------------------------
# Core Application Configuration
# These settings control the fundamental behavior of the application.
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
CLIENT_URL=

# -----------------------------------------------------------------------------
# Security & Authentication Settings
# Critical for protecting your application and user data.
# -----------------------------------------------------------------------------
API_KEY=
JWT_SECRET=

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# Connection details for your PostgreSQL database.
# Uncomment the appropriate URL for your environment (local, docker, production).
# -----------------------------------------------------------------------------
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB_URL=
DOCKER_POSTGRES_URL=
# POSTGRES_DB_URL_PROD= # Example for production URL if you use a separate variable
# POSTGRES_CA_CERT= # Required for production DB connection with SSL/TLS validation. Set directly on hosting provider.

# -----------------------------------------------------------------------------
# External API Keys & Credentials
# Secrets for third-party services.
# -----------------------------------------------------------------------------
TMDB_API_KEY=
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
GOOGLE_BOOKS_API_KEY=
GOOGLE_GEMINI_API_KEY=
DEEPSEEK_API_KEY=

# -----------------------------------------------------------------------------
# Email Configuration
# Settings for sending emails (e.g., password recovery, notifications).
# -----------------------------------------------------------------------------
PASSWORD_APP=
EMAIL_TESTING=

# -----------------------------------------------------------------------------
# Videogames API (Twitch/IGDB)
# Specific credentials for gaming-related APIs.
# -----------------------------------------------------------------------------
VIDEOGAMES_CLIENT_ID=
VIDEOGAMES_CLIENT_SECRET=